// use d3 axis generators
// bottomAxis
// const bottomAxis = d3.axisBottom(xScale);
// g.append('g')
//   .attr('class', 'left axis')
//   .call(bottomAxis);

// const leftAxis = d3.axisLeft(yScale);
// const rightAxis = d3.axisRight(yScale);
// const topAxis = d3.axisTop(xScale);

// d3 tick size and spacing

// tickSizeOuter - affects only the ticks on the edges
// leftAxis.tickSizeOuter(VALUE);

// tickSizeInner - affects inner ticks
// bottomAxis.tickSizeInner(VALUE).tickSize(SIZE)

// const svg = d3.select('#chart-area').append('svg').attr('width', 500).attr('height', 500);

